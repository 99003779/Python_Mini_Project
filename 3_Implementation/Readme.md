# Implementation

## Folder Structure
Folder        | description
--------------| ----------------------------------------------
`inc`         | no header file needed
`src`         | Main python code i.e project.py
`test`        | test.py file
`build`       | Build output (Not included in git)
